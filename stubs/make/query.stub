{{#var queryName = generators.queryName(entity.name)}}
{{#var queryFileName = generators.queryFileName(entity.name)}}
{{{
  exports({
    to: app.makePath(baseDir, entity.path, queryFileName)
  })
}}}
import { Query } from 'adonisjs-cqrs/core'

// Define the result type for this query
export type I{{ queryName }}Result = { success: boolean }

export default class {{ queryName }} extends Query<I{{ queryName }}Result> {
  constructor() {
    super()
  }
}
