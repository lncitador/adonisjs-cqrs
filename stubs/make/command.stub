{{#var commandName = generators.commandName(entity.name)}}
{{#var commandFileName = generators.commandFileName(entity.name)}}
{{{
  exports({
    to: app.makePath(baseDir, entity.path, commandFileName)
  })
}}}
import { Command } from 'adonisjs-cqrs/core'

// Define the result type for this command
export type I{{ commandName }}Result = { success: boolean }

export default class {{ commandName }} extends Command<I{{ commandName }}Result> {
  constructor() {
    super()
  }
}
